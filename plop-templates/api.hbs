import { interceptor } from '@/shared/api/interceptor.api';

import { toQueryString } from '@/shared/utils/url.utils';

export function apiGet{{pascalCase name}}List(searchQueryParams: IApiSearchParams) {
  const params = toQueryString(searchQueryParams);
  return interceptor<I{{pascalCase name}}[]>({
    endpoint: `{{kebabCase name}}?${params}`,
  });
}
